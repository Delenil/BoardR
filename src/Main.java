import java.time.*;
import java.util.*;
public class Main {
    public static void main(String[] args) {
      /* Scanner scr = new Scanner (System.in);

        BoardItem item = new BoardItem("Registration doesn't work", LocalDate.now().plusDays(2));
        item.advanceStatus();

        BoardItem anotherItem = new BoardItem("Encrypt user data", LocalDate.now().plusDays(10));
        BoardItem nextItem = new BoardItem(scr.nextLine(), LocalDate.now().plusDays(30));
        nextItem.advanceStatus();

        Board board = new Board(); //instantiating Board object

        board.items.add(item);
        board.items.add(anotherItem);
        board.items.add(nextItem); //adding BoardItems to list Board


        for (BoardItem boardItem : board.items) {
            boardItem.advanceStatus(); //advances every item from Board list by enum
        }

        nextItem.revertStatus(); //reverts nextItem to Open

        for (BoardItem boardItem : board.items) {
            System.out.println(boardItem.viewInfo());
        }
        //Input: Third Item
        //Output:
        //'Registration doesn't work', [InProgress | 2023-07-14]
        //'Encrypt user data', [ToDo | 2023-07-22]
        //'Third Item', [Open | 2023-08-11]

       */
    }
}